apiVersion: v1
kind: Pod
metadata:
  name: memory-demo
  labels:
    some-label: some-label-value
spec:
  containers:
    - name: maven
      image: maven:3.8.3-jdk-11
      command:
      - cat
      tty: true